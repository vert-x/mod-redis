<assembly>
    <id>${project.groupId}.${project.artifactId}-v${project.version}</id>
    <formats>
        <format>zip</format>
    </formats>
    <baseDirectory>target</baseDirectory>
    <includeBaseDirectory>false</includeBaseDirectory>
    <fileSets>
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>${project.groupId}.${project.artifactId}-v${project.version}/lib</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}/src/main/conf</directory>
            <outputDirectory>${project.groupId}.${project.artifactId}-v${project.version}</outputDirectory>
            <includes>
                <include>mod.json</include>
            </includes>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}</directory>
            <outputDirectory>${project.groupId}.${project.artifactId}-v${project.version}</outputDirectory>
            <includes>
                <include>README.md</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>